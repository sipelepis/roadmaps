# openapi-typescript@7 still pins its peer to typescript ^5.x while the
# workspace is on 6.0.3. The generator only prints a .d.ts — nothing about
# TS 6 breaks it — so accept the stale peer range rather than downgrading.
legacy-peer-deps=true
