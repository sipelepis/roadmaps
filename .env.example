# cp .env.example apps/api/.env and fill in the two keys.
DATABASE_URL=postgresql://rag:rag@localhost:5433/rag
OPENAI_API_KEY=sk-...        # embeddings only (text-embedding-3-small)
ANTHROPIC_API_KEY=sk-ant-... # answer generation (claude-opus-5)
